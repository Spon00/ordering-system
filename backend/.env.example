# Example backend env
# Option A: point to service account file on disk
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json

# Option B: provide raw JSON in an env var (useful for Render)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={...}

PORT=4000
